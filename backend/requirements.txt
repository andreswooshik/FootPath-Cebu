# Keep the LTS minor line while enforcing the latest security-fix baseline.
Django>=5.2.17,<5.3
django-jazzmin~=3.0
djangorestframework~=3.18
firebase-admin>=7.5.0,<8
django-cors-headers~=4.7
# Brute-force / credential-stuffing lockout for the session logins.
django-axes~=8.0
# Argon2id password hashing for the session-authenticated portal accounts.
argon2-cffi~=25.1
python-dotenv~=1.1
# Supabase Storage REST calls (player photo upload + signed URLs).
httpx~=0.28
# Postgres driver — used when DB_* points at Supabase/Postgres.
# SQLite (local dev + tests) needs no driver.
psycopg[binary]~=3.3
# Production WSGI server, static assets, shared Redis cache, and optional
# SENTRY_DSN-driven error monitoring.
gunicorn>=26.2.0,<27
whitenoise>=6.12.0,<7
redis>=8.1.0,<9
sentry-sdk[django]>=2,<3

# Security floors for transitive Firebase/HTTP/Django dependencies. Declaring
# them here prevents a stale environment or image cache from resolving to
# versions with known advisories.
cryptography>=50,<51
h2>=4.4.1,<5
pyasn1>=0.6.4,<0.7
sqlparse>=0.6,<1
