Django~=5.2
django-jazzmin~=3.0
djangorestframework~=3.16
firebase-admin>=6.5,<8
django-cors-headers~=4.7
# Brute-force / credential-stuffing lockout for the session logins.
django-axes~=8.0
# Argon2id password hashing for the session-authenticated portal accounts.
argon2-cffi~=25.1
python-dotenv~=1.1
# Supabase Storage REST calls (player photo upload + signed URLs).
httpx~=0.28
# Postgres driver — only needed when DATABASE_URL points at Supabase/Postgres.
# SQLite (local dev + tests) needs no driver.
psycopg[binary]~=3.2
