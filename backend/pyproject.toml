[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = ["migrations", "node_modules", ".venv", ".runlogs"]

[tool.ruff.lint]
select = ["E9", "F", "I"]

[tool.ruff.format]
quote-style = "single"
