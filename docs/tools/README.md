# Documentation tools

Utilities in this directory generate or visually inspect documentation
artifacts. Run them from the repository root unless their help text says
otherwise. Generated intermediate files belong under the ignored `tmp/`
directory; reviewed outputs belong under `docs/capstone-defense/artifacts/`.
